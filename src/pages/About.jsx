import React from 'react'

const About = () => {
  return (
    <div>About</div>
  )
}

export default About

// className={({ isActive }) => `bg-[#ffff] h-screen w-[230px] fixed top-16 left-0 shadow-xl flex flex-col gap-2 font-[Parkinsans] ${isActive ? 'text-blue-500' : 'hover:text-blue-400'}`}